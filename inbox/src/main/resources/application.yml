logging:
  level:
    root: DEBUG
    reator:
      netty:
        channel: DEBUG
    org:
      springframework:
        web: DEBUG
users-url: http://localhost:9090/users
messages-url: http://localhost:9090/messages